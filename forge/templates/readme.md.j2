# {{ agency_name }}

{{ description }}

**Domain:** {{ domain }}

Generated by [Forge](https://github.com/forge) — AI Agency Meta-Factory.

## Teams

{% for team in teams %}
### {{ team.name }}
{{ team.description }}

{% if team.lead %}
**Lead:** {{ team.lead.name }} ({{ team.lead.title }})
{% endif %}

**Agents:**
{% for agent in team.agents %}
- **{{ agent.name }}** — {{ agent.title }} ({{ agent.role }})
{% endfor %}
{% endfor %}

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Set your API key
export OPENAI_API_KEY="your-key"

# Run interactively
python main.py

# Or run as API server
uvicorn api_server:app --host 0.0.0.0 --port 8000
```

## API Endpoints

- `POST /api/task` — Execute a task
- `GET /api/status` — Get agency status
- `GET /health` — Health check
{% for endpoint in api_endpoints %}
- `{{ endpoint.method }} {{ endpoint.path }}` — {{ endpoint.description }}
{% endfor %}

## Docker

```bash
docker compose up --build
```

## Configuration

| Variable | Description | Default |
|----------|-------------|---------|
| `OPENAI_API_KEY` | OpenAI API key | Required |
| `OPENAI_BASE_URL` | Custom API endpoint | OpenAI default |
| `FORGE_MODEL` | LLM model | {{ model }} |
{% for key, val in env_vars.items() %}
| `{{ key }}` | {{ key | replace('_', ' ') | title }} | {{ val }} |
{% endfor %}
